Prime Hub V5.4.2 — Order History Command Hotfix

Changes:
- Customers can use BOTH /order and /orders.
- The home button is labeled "Order History".
- Customer order history still shows completed/delivered purchases only.
- Admin recent-orders command is renamed from /orders to /adminorders to avoid any command collision.

Customer:
  /order
  /orders

Admin:
  /adminorders
